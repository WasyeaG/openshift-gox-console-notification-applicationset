applicationset:
  name: gox-prd-console-notification
  namespace: openshift-gitops

source:
  targetRevision: main

clusterSelector:
  matchLabels:
    gox.li9.com/environment: prd

environment:
  name: prd
  valueFiles:
    - values-common.yaml
    - values-prd.yaml
